# Git LFS Configuration for RipCatch
# Large File Storage for video and large image files

# Track large video files with Git LFS
*.mp4 filter=lfs diff=lfs merge=lfs -text
RipCatch-v2.0/Results/*.mp4 filter=lfs diff=lfs merge=lfs -text

# Track large model weights with Git LFS (if needed)
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text

# Track large images over 50MB with Git LFS
RipCatch-v2.0/Results/inference_results.png filter=lfs diff=lfs merge=lfs -text

